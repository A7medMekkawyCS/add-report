{
  "name": "add-info-report",
  "version": "2.0.0",
  "description": "Multi-user GitLab to Odoo timesheet automation with admin dashboard",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "seed": "node src/seed.js",
    "test": "node --test tests/**/*.test.js",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "odoo": "node odoo-timesheet-puppeteer.js",
    "commits": "node gitlab-commits.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "mongoose": "^8.24.4",
    "puppeteer": "^24.6.1"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
